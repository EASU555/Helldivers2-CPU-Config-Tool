# 使用说明

## 使用前

1. 正常关闭《地狱潜兵 2》。
2. 打开 `地狱潜兵CPU修改器.exe`。
3. 工具会优先恢复上次使用的目录；目录无效时再尝试从 Steam 自动定位。
4. 未找到时，点击“浏览”并选择 `Helldivers 2` 安装目录。

## 应用方案

1. 首次建议使用“主方案：U9-275HX 稳定 8P”。
2. 确认线程摘要中的逻辑线程、当前保留线程和游戏预计可用线程。
3. 选择是否修改 CPU、音频，以及是否在成功后把配置设为只读。
4. 点击“应用修改”，检查预览后确认。
5. 也可点击“应用修改并启动游戏”；只有修改和校验成功后才会调用 Steam。

程序会按以下顺序处理真实修改：创建备份、写入同目录临时文件、校验临时文件、原子替换、再次校验。替换后的任意失败都会尝试恢复修改前备份。配置内容已是目标值时不会重复写入或创建备份。

## 更新后检查

启动工具时会静默比较当前配置与所选方案，不弹出消息框。状态显示“修复失效”时，说明参数可能被 Steam 更新覆盖。点击“检测失效”可查看详细差异，再重新应用即可。

## 文件锁定

- 勾选“修改完成后将配置文件设为只读”：只有修改和校验成功后才锁定。
- 未勾选：修改前是可写就保持可写，修改前是只读就恢复只读。
- 点击“一键解除只读”：只解除配置文件的只读属性，不修改参数。

锁定可能阻止游戏自行保存该文件。游戏更新、验证文件或需要更改设置前，可以先解除只读。

## 恢复

点击“恢复备份”会恢复最近一份 `settings.ini.bak_yyyyMMdd_HHmmss_fff`。恢复前程序创建临时保护副本，恢复后校验关键参数；失败时回到恢复操作前的内容。自动备份最多保留最近 10 份。

“恢复常见默认值”会写入：

```text
num_reserved_threads = 2
memory_size = 26214400
num_refills_in_voice = 2
```

这些是常见默认值，不保证等于未来版本官方值。需要绝对原始值时，优先恢复修改前备份或通过 Steam 验证文件。

## 参数边界

- 保留线程数必须小于 CPU 逻辑线程数。
- 程序最多允许保留到“逻辑线程数减 4”，保证游戏至少有 4 个预计可用线程。
- 对 U9-275HX，超过 8 个游戏可用线程的方案会标记为偏激进。
- 偏激进方案可能提高部分场景峰值帧率，也可能提高温度或增加帧时间波动。

## 诊断信息

“复制诊断信息”包含工具/系统/CPU、脱敏路径、当前与目标参数、最后检测和错误信息。用户目录会显示为 `%USERPROFILE%`，不会上传或自动发送任何数据。

## 本地设置

工具设置保存在：

```text
%LOCALAPPDATA%\Helldivers2CpuFixer\user-settings.ini
```

文件损坏或数值无效时，程序会使用安全默认设置启动。
