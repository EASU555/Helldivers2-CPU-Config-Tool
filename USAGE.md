# 使用说明

## 使用前

1. 关闭《地狱潜兵 2》。
2. 启动由源码构建的工具。
3. 工具会尝试自动定位游戏目录；未找到时，手动选择 `Helldivers 2` 的安装目录。

## 应用方案

1. 在下拉列表选择一个方案。默认方案面向 Ultra 9 275HX 一类的 24 线程混合架构 CPU，保留 16 个线程，使游戏主要使用约 8 个线程。
2. 点击“应用修改”。
3. 在写入预览中确认目标文件和参数，再确认执行。
4. 每次写入前，工具会备份现有 `data/settings.ini`。

## 更新后检查

Steam 或游戏更新后可使用工具中的“检测失效”功能，确认当前设置是否仍与所选方案一致。若被覆盖，重新应用方案即可。

## 恢复方式

- 使用“恢复备份”还原最近一次 `settings.ini` 备份。
- 或选择“恢复官方默认”方案并应用。

## 风险说明

参数并不适合所有机器。保留线程过少可能降低帧率，保留线程过少以外的激进配置也可能增加温度或调度压力。每次只调整一个方案，并在实际游戏场景中观察稳定性。
